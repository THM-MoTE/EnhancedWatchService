apply plugin: 'java'
apply plugin: 'eclipse'
apply plugin: 'application'

repositories {
  mavenCentral()
}

dependencies {
  compile group: "ch.qos.logback", name: "logback-classic", version: "1.1.3"
  runtime files("logback.xml")
}

mainClassName = 'Main'
